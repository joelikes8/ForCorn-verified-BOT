# Required Discord configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Database configuration (choose one)
# For PostgreSQL (preferred for production):
DATABASE_URL=postgresql://username:password@host:port/database
# For SQLite (development/fallback):
# DATABASE_URL=sqlite:///forcorn_bot.db

# Optional configurations
# SESSION_SECRET=your_flask_session_secret_here
# PORT=5000  # Custom port for web interface

# Workflow control flags
# Set to "true" to disable web interface:
# NO_WEB_SERVER=true
# Set to "true" to indicate bot is running in a workflow:
# DISCORD_BOT_WORKFLOW=true
# Set to "true" for complete isolation:
# BOT_ONLY_MODE=true
